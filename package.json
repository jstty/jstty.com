{
  "name": "jstty.com",
  "version": "0.1.0",
  "author": {
    "name": "Joseph E. Sutton",
    "email": "joe@jstty.com",
    "url": "http://www.jstty.com"
  },
  "maintainers": [
    {
      "name": "jstty",
      "email": "joe@jstty.com"
    }
  ],
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.3.11",
    "co": "^4.6.0",
    "ejs": "^2.3.4",
    "hyper.io": "^0.5.0-alpha4",
    "lodash": "^3.10.1",
    "moment": "^2.11.1",
    "pdfmake": "^0.1.18"
  },
  "devDependencies": {
    "gulp": "^3.9.0"
  },
  "scripts": {
    "clean": "cd frontend && rm -rf node_modules && rm -rf dist && cd ../server && rm -rf node_modules && cd .. && rm -rf node_modules",
    "init": "npm install && cd frontend && npm install && cd ../server && npm install",
    "build": "cd frontend && npm run build",
    "frontend": "cd frontend && node webpack.server.js",
    "predeploy": "npm run init && npm run build",
    "deploy": "eb deploy",
    "start": "node app"
  },
  "engines": {
    "node": ">=4.0"
  }
}
