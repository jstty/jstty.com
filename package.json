{
  "name": "jstty.com",
  "version": "0.1.0",
  "author": {
    "name": "Joseph E. Sutton",
    "email": "joe@jstty.com",
    "url": "http://www.jstty.com"
  },
  "maintainers": [
    {
      "name": "jstty",
      "email": "joe@jstty.com"
    }
  ],
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "gulp": "^3.9.0"
  },
  "scripts": {
    "clean": "cd frontend && rm -rf node_modules && rm -rf dist && cd ../server && rm -rf node_modules && cd .. && rm -rf node_modules",
    "init": "npm install && cd frontend && npm install && cd ../server && npm install",
    "build": "cd frontend && ./node_modules/.bin/webpack",
    "deploy": "eb deploy",
    "prestart": "npm run init && npm run build",
    "start": "cd server && node server.js"
  },
  "engines": {
    "node": ">=4.0"
  }
}
